/**
 * Part 2 – Child class representing General Practitioners
 * Extends HealthProfessional to inherit common attributes and adds GP-specific features
 * Demonstrates inheritance by specializing the base class for a specific role
 */
// Part 2 – Child class (General Practitioner)
public class GeneralPractitioner extends HealthProfessional {
    // Part 2 - Additional instance variables to differentiate from other health professionals
    private boolean canPrescribeMedication; // Whether GP can prescribe medication
    private int maxPatientsPerDay;         // Maximum number of patients GP can see daily

    /**
     * Part 2 - Default constructor
     * Calls parent constructor and initializes GP-specific attributes with default values
     */
    public GeneralPractitioner() {
        super(); // Call parent class constructor
        this.canPrescribeMedication = true;
        this.maxPatientsPerDay = 20;
    }

    /**
     * Part 2 - Parameterized constructor that initializes all instance variables
     * Calls parent constructor and sets GP-specific attributes
     *
     * @param id the unique identifier
     * @param name the full name
     * @param specialization the medical specialization
     * @param canPrescribeMedication prescription authority flag
     * @param maxPatientsPerDay daily patient capacity
     */
    public GeneralPractitioner(int id, String name, String specialization,
                               boolean canPrescribeMedication, int maxPatientsPerDay) {
        super(id, name, specialization); // Call parent constructor with parameters
        this.canPrescribeMedication = canPrescribeMedication;
        this.maxPatientsPerDay = maxPatientsPerDay;
    }

    /**
     * Part 2 - Method that prints health professional details including type
     * Overrides/extends parent functionality to include GP-specific information
     * Demonstrates polymorphism by providing specialized implementation
     */
    public void printProfessionalDetails() {
        System.out.println("=== General Practitioner Details ===");
        System.out.println("Type: General Practitioner");
        super.printDetails(); // Reuse parent class functionality
        System.out.println("Can Prescribe Medication: " + canPrescribeMedication);
        System.out.println("Max Patients Per Day: " + maxPatientsPerDay);
        System.out.println("-----------------------------------");
    }

    // Getter and setter methods for GP-specific attributes
    public boolean canPrescribeMedication() {
        return canPrescribeMedication;
    }

    public void setCanPrescribeMedication(boolean canPrescribeMedication) {
        this.canPrescribeMedication = canPrescribeMedication;
    }

    public int getMaxPatientsPerDay() {
        return maxPatientsPerDay;
    }

    public void setMaxPatientsPerDay(int maxPatientsPerDay) {
        this.maxPatientsPerDay = maxPatientsPerDay;
    }
}
